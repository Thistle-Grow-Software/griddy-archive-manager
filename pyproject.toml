[project]
name = "griddy-archive-manager"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "beautifulsoup4>=4.14.3",
    "boto3>=1.35.0",
    "django>=6.0.1",
    "django-click>=2.5.0",
    "django-filter>=25.2",
    "django-stubs>=5.2.9",
    "djangorestframework>=3.17.1",
    "griddy>=0.45.0,<1.0",
    "pillow>=12.1.0",
    "psycopg>=3.3.2",
    "pyjwt[crypto]>=2.10.0",
    "pytest-cov>=7.0.0",
    "requests>=2.32.5",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-django>=4.9",
    "ruff>=0.11",
    "django-debug-toolbar>=6.2.0",
    "django-extensions>=4.1",
    "ipython>=9.10.0",
    "drf-spectacular>=0.29.0",
    "djangorestframework-stubs>=3.16.9",
]
docs = [
    "zensical>=0.0.28",
    "mkdocstrings[python]>=0.29.0",
]

[[tool.uv.index]]
name = "private-registry"
url = "https://thistle-grow-685711685350.d.codeartifact.us-east-1.amazonaws.com/pypi/griddy/simple/"

[tool.ruff]
line-length = 88
target-version = "py314"

[tool.ruff.lint]
select = [
    "E",      # pycodestyle errors
    "F",      # pyflakes
    "W",      # pycodestyle warnings
    "I",      # isort
    "B",      # flake8-bugbear
    "UP",     # pyupgrade (modernize syntax)
    "DJ",     # flake8-django (Django-specific rules)
    "SIM",    # flake8-simplify
    "RUF",    # Ruff-specific rules
    "C4",     # flake8-comprehensions
    "DTZ",    # flake8-datetimez (catches naive datetime issues — big in Django)
    "T20",    # flake8-print (catches stray print statements)
    # "PT",     # flake8-pytest-style (if using pytest-django)
]
ignore = [
    "E501",
    "DJ001"
]

[tool.ruff.lint.per-file-ignores]
"**/models.py" = ["RUF012"]
"**/serializers/**" = ["RUF012"]       # DRF Meta.fields convention
"**/viewsets/**" = ["RUF012"]          # DRF class-level list attributes
"**/filters.py" = ["RUF012"]          # django-filter Meta.fields convention
"**/migrations/*.py" = ["ALL"]  # auto-generated, don't lint heavily
"**/settings/*.py" = ["F405", "E501"]                 # wildcard imports common in settings
"**/settings.py" = ["F405", "E501"]
"**/tests/**" = ["S101"]                      # assert is fine in tests
"scripts/**" = ["T201"]                       # standalone CLIs print to stdout

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "gam.settings_ci"
python_files = ["tests.py", "test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "--no-migrations"
markers = [
    "enforce_api_permissions: do not bypass HasAPIPermission for this test",
    "smoke: live external-dependency smoke test; skipped unless explicitly opted in",
]
